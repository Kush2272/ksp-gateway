//! Integration test harness for KSP Gateway.
//!
//! Tests in this crate spin up real gateway components and test them end-to-end.
//! Individual unit tests live in each crate's own `tests/` or inline `#[cfg(test)]`.

// This file intentionally left minimal — tests are in tests/ subdirectory.
